# Coffee Ledger PWA インストール&使用ガイド

## 📱 PWA機能

Coffee LedgerはProgressive Web App (PWA)として設計されており、以下の機能を提供します：

- **オフライン対応**: 一度訪問したページは、インターネット接続がなくても表示可能
- **ホーム画面への追加**: スマートフォンのホーム画面にアプリとして追加可能
- **プッシュ通知**: 将来的な機能拡張に対応
- **バックグラウンド同期**: データの自動同期（開発中）

## 🚀 インストール方法

### iPhone/iPad (Safari)

1. SafariでCoffee Ledgerを開く
2. 共有ボタン（□と↑）をタップ
3. 「ホーム画面に追加」を選択
4. 名前を確認して「追加」をタップ

### Android (Chrome)

1. ChromeでCoffee Ledgerを開く
2. メニュー（⋮）をタップ
3. 「ホーム画面に追加」を選択
4. 「追加」をタップ

### デスクトップ (Chrome/Edge)

1. ブラウザでCoffee Ledgerを開く
2. アドレスバーの右側のインストールアイコン（＋）をクリック
3. 「インストール」をクリック

## 🔧 トラブルシューティング

### Service Workerのリセット

#### Safari (iPhone/iPad)
1. 設定 → Safari → 詳細 → Webサイトデータ
2. Coffee Ledgerのデータを削除
3. ページを再度開く

#### Chrome (Android/Desktop)
1. DevTools (F12) → Application → Service Workers
2. 「Unregister」をクリック
3. ページをリロード

### オフライン時の動作

- 静的アセット（画像、アイコン）は自動的にキャッシュ
- 一度訪問したページはオフラインでも表示可能
- 新規データの追加・編集はオンライン時に同期

## 📊 キャッシュ戦略

| コンテンツタイプ | 戦略 | 説明 |
|---|---|---|
| 画像・アイコン | Cache First | キャッシュ優先、高速表示 |
| HTMLページ | Network First | 最新コンテンツ優先、オフライン時はキャッシュ |
| アプリデータ | IndexedDB | 端末内のみ。クラウド同期なし |
| 静的アセット | Cache First | 一度キャッシュしたら再利用 |

## 🔒 セキュリティとプライバシー

- サーバーサイド認証は未実装
- ローカルデータ（IndexedDB）は端末内のみで保存

## 💡 使用上のヒント

1. **初回アクセス時**: インターネット接続が必要
2. **2回目以降**: オフラインでも基本機能が利用可能
3. **データ同期**: オンライン復帰時に自動同期
4. **更新確認**: 定期的にリロードして最新版を取得

## 🔄 Service Worker バージョン管理

現在のバージョン: `v6`

Service Workerは自動的に更新されますが、手動で更新する場合：
1. ブラウザのキャッシュをクリア
2. Service Workerをアンインストール
3. ページをリロード

## 📝 既知の問題

- オフライン時は新規データの保存ができない
  → 将来的にバックグラウンド同期で対応予定

## 🚧 今後の改善予定

- [ ] オフラインでのデータ作成・編集機能
- [ ] バックグラウンド同期の完全実装
- [ ] プッシュ通知によるリマインダー機能
- [ ] より詳細なオフライン表示の改善